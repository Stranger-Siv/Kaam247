# Client Environment Variables (Optional for local development)
# Copy this file to .env.local if you want to override defaults
# The app automatically uses http://localhost:3001 in development

# API Base URL (defaults to http://localhost:3001 in development)
# VITE_API_BASE_URL=http://localhost:3001

# Socket URL (defaults to http://localhost:3001 in development)
# VITE_SOCKET_URL=http://localhost:3001

# Google OAuth Client ID (Web) from Google Cloud Console > APIs & Credentials
# Required for "Continue with Google" on Login/Register. Same value as GOOGLE_CLIENT_ID on server.
# VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
