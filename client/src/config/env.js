// Environment configuration
// All environment variables must be prefixed with VITE_ to be accessible in the browser

export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'https://kaam247.onrender.com'
export const SOCKET_URL = import.meta.env.VITE_SOCKET_URL || 'https://kaam247.onrender.com'

// Configuration loaded silently

